services:
  db:
    container_name: ${DB_HOST}
    image: postgres:15
    environment:
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_DB=${DB_NAME}
    ports:
     - ${DB_PORT}:${DB_PORT}
    restart: always
    