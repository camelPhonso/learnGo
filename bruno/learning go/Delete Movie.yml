info:
  name: Delete Movie
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/movies/:movieId"
  params:
    - name: movieId
      value: "{{testMovieId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
