info:
  name: Select All Movies
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/movies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
